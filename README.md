# Jax - MINST .exe 

PURPOSE : Load MNIST, explore its structure,
          and visualize samples using JAX + matplotlib

STEPS   :
  1. Load MNIST via tensorflow_datasets (easiest in Codespaces)
  2. Inspect shape, dtype, min/max values with JAX
  3. Visualize a grid of sample images
  4. Plot pixel intensity distribution with JAX